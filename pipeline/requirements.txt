# !! Requires Python 3.11 or 3.12. Python 3.13 is NOT supported by WhisperX/PyAnnote yet.
# Install PyTorch FIRST from https://pytorch.org/get-started/locally/ (CUDA or CPU)
# torch>=2.2.0
# torchaudio>=2.2.0

# Transcription
faster-whisper>=1.0.3
whisperx>=3.3.1

# Diarization — requires HuggingFace token + model agreement
# Accept terms at: https://hf.co/pyannote/speaker-diarization-3.1
pyannote.audio>=3.3.2

# Audio
soundfile>=0.12.1
numpy>=1.26,<2.1        # 1.26.x = last 1.x; 2.0/2.1 also work but check torch compat

# Summarization (Ollama must be running locally: https://ollama.ai)
ollama>=0.3.3

# Utilities
tomli>=2.0.1; python_version < "3.11"
structlog>=24.1.0
